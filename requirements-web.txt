# Commander AI Lab — Web server dependencies (Phase 5)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0          # async Scryfall art proxy
websockets>=12.0       # used by uvicorn WebSocket transport
pydantic>=2.6.0

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27.0          # TestClient uses httpx transport
